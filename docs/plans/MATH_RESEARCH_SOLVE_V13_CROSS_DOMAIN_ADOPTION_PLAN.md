# math-research-solve v13 来源边界与延期说明

- 状态：`SOURCE_EXCLUDED / PLANNING_ONLY / DEFERRED`
- 版本：v2.0-oss-r0
- 状态同步日期：2026-08-23
- 当前不可变基线：`math-research-solve` v8/1.0.1

> 本文件仅记录来源边界、排除决定和未来重新评估 Gate。它不包含或复述
> 外部实现设计，不授权复制、安装、实现、迁移或激活 v13/Pika 内容。

## 1. 本轮可确认的事实

- 历史审查输入曾以 `pika_math_learning_toolkit-1.11.zip` 标识，其中包含被称为
  `math-research-solve v13` 的组件。
- OSS-R0 审计期间未在当前工作区或安全临时目录找到原始 ZIP；因此，
  **本轮无法重新核验原始 artifact**。
- 本轮不重新声称任何历史文件数、内容分类数量、哈希或许可结论。
- 当前没有可支持复制、再分发或衍生改写的明确许可证证据。
- 合并前基线仓库的 tracked-file 扫描未发现 Pika/v13 代码、schema、fixture、
  模板或 Skill payload；相关内容仅出现在规划文档和来源边界记录中。

## 2. OSS-R0 处理决定

此前的详细机制计划无法在缺少原始 artifact 与许可证的条件下重新证明来源和
独立表达，因此已由本说明完整替换。当前仓库：

- 不分发外部 ZIP、Skill payload、源码、schema、fixture 或模板；
- 不保留外部材料的详细机制说明、翻译、近义改写或长段摘录；
- 只保留识别来源风险所需的最少事实与明确排除决定；
- 将该例外登记在 `docs/governance/SOURCE_PROVENANCE.json`；
- 不把本次文档收口解释为 v13 评估、验收、采用或实现。

因此，本文件在来源清单中归为项目独立撰写的治理记录，而不是第三方内容复用。

## 3. 不可变边界

- `math-research-solve` v8/1.0.1 基线保持不可变；不得用 v13 静默覆盖。
- `skills/staging/` 的本地忽略资产不得因本计划而删除、安装、提交或同步。
- Phase 6、真实 ML/DL 执行器、Skill 安装/激活和生产科研能力均不在本文件范围。
- 没有另行授权时，不获取、解包、运行或安装任何 Pika/v13 payload。

## 4. 未来重新评估 Gate

只有以下条件全部满足，才可提出新的、独立 PR 计划：

1. 原始 artifact 由权利持有人或获授权来源直接提供；
2. 记录来源、版本、获取日期、SHA-256 与完整许可证；
3. 权利审查确认允许目标使用、修改和再分发，并确定 NOTICE/署名义务；
4. 在仓库外隔离审查，不向当前源码树复制 payload 或表达性内容；
5. 若仅采用抽象思想，基于本项目自身需求重新撰写 ADR，并提供独立来源说明；
6. 用户另行明确授权 v13 的评估或实施范围；
7. 新工作不得改变 v8/1.0.1 基线，也不得把工程测试升级为真实科研证据。

任一条件未满足时，状态保持 `SOURCE_EXCLUDED / DEFERRED`。
